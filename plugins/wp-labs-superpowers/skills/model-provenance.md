# Team workflow: record the model in spec/plan documents

This is a record of provenance, not a design field: never treat it as a requirement or constraint
on later steps (a plan's executor, for instance, may run under a different model than the plan's
author, and that's fine).
