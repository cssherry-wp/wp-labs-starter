# Team workflow: record the model in report headers

This is a record of provenance, not a design field: never treat it as a requirement or constraint
on later steps. A finding dispatched to `--fix` may be applied by a different model than the one
that wrote the review — see `change-review/review-lifecycle.md`'s Disposition rules for that case.
